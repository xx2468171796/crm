# ============================================
# ANKOTTI CRM 环境变量配置示例
# ============================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 根据实际环境修改配置
# 3. 在 1Panel 中配置环境变量
# ============================================

# ============================================
# 数据库配置
# ============================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ankotti_crm
DB_USERNAME=your_username
DB_PASSWORD=your_password

# ============================================
# S3/MinIO 对象存储配置
# ============================================
# 存储驱动：local 或 s3
STORAGE_DRIVER=s3

# S3 端点（包含协议）
# MinIO: http://your-minio-ip:9000
# 阿里云 OSS: https://oss-cn-hangzhou.aliyuncs.com
# 腾讯云 COS: https://cos.ap-guangzhou.myqcloud.com
S3_ENDPOINT=https://your-minio-endpoint.com

# S3 区域
S3_REGION=cn-default

# S3 存储桶名称
S3_BUCKET=your-bucket

# S3 访问密钥
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key

# 路径样式：1=路径样式(MinIO), 0=虚拟主机样式(AWS/阿里云)
S3_USE_PATH_STYLE=1

# 强制 HTTPS：1=是, 0=否（不设置则从 endpoint 自动检测）
# S3_USE_HTTPS=1

# 存储前缀（可选）
# S3_PREFIX=

# 超时时间（秒）
S3_TIMEOUT=60

# 公开访问URL（用于客户门户等公开链接）
# 格式: http(s)://域名:端口/bucket
# 不设置则自动拼接 endpoint + bucket
# S3_PUBLIC_URL=http://your-public-url.com/bucket

# ============================================
# 应用配置
# ============================================
# 调试模式：true=开发环境, false=生产环境
APP_DEBUG=false

# 时区
APP_TIMEZONE=Asia/Shanghai

# ============================================
# WebSocket 配置（可选）
# ============================================
# WS_PORT=8300
# WS_PUSH_PORT=8301
# WS_ENABLED=true
